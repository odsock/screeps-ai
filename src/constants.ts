class Constants {
  static readonly MAX_CREEPS: number = 4;
  static readonly BODY_WORKER: BodyPartConstant[] = [WORK, WORK, WORK, WORK, WORK, MOVE, MOVE, MOVE, MOVE, MOVE, CARRY];
}
export default Constants;